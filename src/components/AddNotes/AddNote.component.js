import React from 'react'
import { View, Text } from 'react-native'

const AddNote = (navigate) => {
  // console.log('??navigate2??', JSON.stringify(navigate, null, 2))
  return (
    <View>
      <Text>AddNote</Text>
    </View>
  )
}

export default AddNote
